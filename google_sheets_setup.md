# 📊 คู่มือการเชื่อมต่อ Google Sheets & Google Drive สำหรับสินค้า

ระบบเชื่อมต่อ Google Sheets นี้ช่วยให้คุณสามารถ **เพิ่ม, แก้ไข, ลบ หรือซ่อนสินค้า และหมวดหมู่** ได้โดยตรงจาก Google Sheets โดยไม่ต้องแตะต้องโค้ดของเว็บไซต์!

---

## 🚀 โครงสร้าง Google Sheet (ไม่ต้องมีราคาแล้ว)

### กำหนด Header ใน แถวแรก (Row 1) ดังนี้:

| slug | name | brand | category | image1 | image2 | image3 | description | shopee | lazada | tiktok | official | status |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| cute-cat-bag | กระเป๋าน้องแมวสุดคิวท์ | MINISO | แฟชั่น | `https://drive.google.com/...` | `https://drive.google.com/...` | `https://drive.google.com/...` | ไอเทมน่ารัก... | `https://shopee...` | `https://lazada...` | | | active |

> 💡 **อธิบายรายละเอียดคอลัมน์:**
> - `slug` (ถ้าเว้นว่างไว้ ระบบจะสร้างให้อัตโนมัติจากชื่อสินค้า)
> - `name` (ชื่อสินค้า)
> - `brand` (ชื่อแบรนด์/ร้านค้า)
> - `category` (หมวดหมู่สินค้า - **ถ้านำชื่อหมวดหมู่ใหม่มาใส่ ระบบจะเพิ่มแท็บหมวดหมู่นั้นเข้าเว็บให้อัตโนมัติ!**)
> - `image1` (รูปภาพที่ 1 - รูปหลักแสดงในหน้าแรก/หน้ารวมสินค้า)
> - `image2` (รูปภาพที่ 2 - แสดงในแกลเลอรีหน้ารายละเอียด)
> - `image3` (รูปภาพที่ 3 - แสดงในแกลเลอรีหน้ารายละเอียด)
> - `description` (รายละเอียดสินค้า)
> - `shopee` / `lazada` / `tiktok` / `official` (ลิงก์สั่งซื้อสินค้าของแต่ละแพลตฟอร์ม)
> - `status` (ใส่ `active` เพื่อแสดง หรือใส่ `hidden` / `deleted` / `ซ่อน` / `ลบ` เพื่อซ่อนสินค้า)

---

## 🖼️ การฝากรูปภาพผ่าน Google Drive (รองรับได้สูงสุด 3 รูป)

ระบบรองรับการนำลิงก์รูปจาก **Google Drive** มาวางในช่อง `image1`, `image2`, `image3` ได้ทันที โดยระบบจะแปลงเป็น Direct Image URL ให้อัตโนมัติ:

### วิธีเอาลิงก์จาก Google Drive:
1. อัปโหลดรูปภาพเข้า Google Drive
2. คลิกขวาที่ไฟล์รูป -> เลือก **แชร์ (Share)** -> เปลี่ยนสิทธิ์เป็น **"ทุกคนที่มีลิงก์ (Anyone with the link)"**
3. คัดลอกลิงก์มาวางในช่อง `image1`, `image2`, หรือ `image3` ใน Google Sheet ได้เลย!
   *(ตัวอย่างลิงก์: `https://drive.google.com/file/d/1A2B3C4D5E6F/view?usp=sharing`)*

---

## 🔑 การเชื่อมต่อ Sheet กับเว็บไซต์

1. คัดลอก **Sheet ID** จาก URL ของ Google Sheet
   *ตัวอย่าง URL:* `https://docs.google.com/spreadsheets/d/`**`1a2b3c4d5e6f7g8h9i0j`**`/edit`
   *(ค่า Sheet ID คือส่วนตัวอักษรระหว่าง `/d/` และ `/edit`)*
2. เปิดไฟล์ **`.env.local`** ในโฟลเดอร์โปรเจกต์ แล้วใส่ค่า:
   ```env
   GOOGLE_SHEET_ID=1a2b3c4d5e6f7g8h9i0j
   ```
3. กด **แชร์ (Share)** ที่มุมขวาบนของ Google Sheet -> เปลี่ยนสิทธิ์เป็น **"ทุกคนที่มีลิงก์มีสิทธิ์ดู (Anyone with the link can view)"**

---

## ⚡ คุณสมบัติของระบบ (Features)

- 🖼️ **รองรับรูปภาพ 3 รูป**: แสดงแกลเลอรีรูปภาพสินค้าในหน้ารายละเอียดสินค้า
- ❌ **ไม่มีช่องราคา (Price Removed)**: เน้นแสดงข้อมูลสินค้าและปุ่มลิงก์สั่งซื้อไปยัง Shopee / Lazada / TikTok / Official Store
- 🔄 **Revalidate Cache 60 วินาที**: โหลดข้อมูลได้รวดเร็วทันใจ
- 🏷️ **Dynamic Categories**: สร้างหมวดหมู่อัตโนมัติตามที่มีอยู่ใน Sheet
- 🗑️ **เพิ่ม / ลบ สินค้าสะดวก**: เพิ่มแถวใหม่ หรือเปลี่ยน `status` เป็น `hidden` / `ซ่อน` ได้ทันที
